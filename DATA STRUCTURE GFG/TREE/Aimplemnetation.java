
public class Aimplemnetation {
    
   

    class Node{
        int key;
        Node left;
        Node right;

        Node(int key){
            this.key = key;
            this.left = null;
            this.right = null;

        }
    }

    static int print(Node root){
        Node newNode = 
    }

    public static void main(String[] args) {

        
        Node root = new Node(10);
        Node root.left = new Node(20);
        Node root.right = new Node(30);
        Node root.left.left = new Node(40);
       
    }
}
